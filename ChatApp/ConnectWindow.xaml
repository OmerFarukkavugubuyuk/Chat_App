<Window x:Class="ChatApp.ConnectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LAN Sohbet" Height="340" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#1e1e2e">
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="Foreground" Value="#cdd6f4"/>
            <Setter Property="BorderBrush" Value="#45475a"/>
            <Setter Property="Padding" Value="6,5"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#a6adc8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="0,0,0,2"/>
        </Style>
    </Window.Resources>
    <StackPanel Margin="28,20">
        <TextBlock Text="LAN Sohbet" FontSize="22" FontWeight="Bold"
                   Foreground="#cdd6f4" Margin="0,0,0,20"/>

        <Label Content="Kullanıcı Adı"/>
        <TextBox x:Name="NickBox" Margin="0,0,0,14"/>

        <Label Content="Port"/>
        <TextBox x:Name="PortBox" Text="8888" Margin="0,0,0,14"/>

        <!-- Sadece istemci modunda görünür -->
        <StackPanel x:Name="IpPanel" Visibility="Collapsed">
            <Label Content="Sunucu IP Adresi"/>
            <TextBox x:Name="IpBox" Margin="0,0,0,14"/>
        </StackPanel>

        <Grid Margin="0,6,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Sunucu Başlat" Click="ServerButton_Click"
                    Background="#a6e3a1" Foreground="#1e1e2e" FontWeight="Bold"
                    BorderThickness="0" Padding="0,10" FontSize="13" Cursor="Hand"/>
            <Button Grid.Column="2" Content="Bağlan" Click="ConnectButton_Click"
                    Background="#89b4fa" Foreground="#1e1e2e" FontWeight="Bold"
                    BorderThickness="0" Padding="0,10" FontSize="13" Cursor="Hand"/>
        </Grid>

        <TextBlock x:Name="ErrorText" Foreground="#f38ba8" FontSize="11"
                   Margin="0,10,0,0" TextWrapping="Wrap"/>
    </StackPanel>
</Window>
